import styled from '@emotion/native';

export const RegisterOwner = styled.View`
  padding: 10px;
  background-color: ${({ theme }) => theme.colors.gc_4};
`;

export const AddAvatarBtn = styled.Pressable`
  align-items: center;
`;
