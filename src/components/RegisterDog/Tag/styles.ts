import styled from '@emotion/native';

export const Tag = styled.View`
  border: solid 1px ${({ theme }) => theme.colors.gc_1};
  padding: 5px 12px 6px 12px;
  border-radius: 30px;
  height: 32px;

  justify-content: center;
  align-items: center;
`;
